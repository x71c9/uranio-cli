import {
	init,
	// transpose,
	// alias,
	// hooks,
	// build,
} from './cmd/';

export default {
	init: init
	// init: init.run,
	// transpose: transpose.run,
	// alias: alias.run,
	// hooks: hooks.run,
	// build: build.run,
};
