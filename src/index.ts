import {
	init,
	alias,
	// build,
	// transpose,
	// hooks,
} from './cmd/';

export default {
	init: init,
	alias: alias,
	// build: build.run,
	// transpose: transpose.run,
	// hooks: hooks.run,
};
